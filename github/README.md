# 📁 Папка GitHub - Инструкции и файлы для GitHub Actions

## ⚠️ Важная информация

**Файлы `.github/workflows/` должны быть в корне проекта**, а не в этой папке!

Эта папка содержит:
- 📄 Инструкции по настройке
- 📄 Шаблоны файлов
- 📄 Документацию

## 📋 Структура файлов

```
ваш-проект/
├── .github/                    ← Должен быть в КОРНЕ проекта
│   └── workflows/
│       └── deploy-functions.yml
├── github/                     ← Эта папка (инструкции)
│   ├── README.md              ← Этот файл
│   ├── SETUP_INSTRUCTIONS.md  ← Подробная инструкция
│   └── files/                 ← Копии файлов для справки
├── functions/
│   ├── index.js
│   └── package.json
├── .firebaserc                 ← Должен быть в КОРНЕ проекта
└── firebase.json
```

## 🚀 Быстрый старт

### Шаг 1: Получите Firebase Token

Когда у вас есть доступ к интернету:

```bash
npm install -g firebase-tools
firebase login:ci
```

Скопируйте полученный токен.

### Шаг 2: Создайте GitHub репозиторий

1. Откройте [GitHub](https://github.com/)
2. Создайте новый **публичный** репозиторий
3. Название: например, `imtahan-plus`

### Шаг 3: Добавьте Firebase Token в GitHub Secrets

1. GitHub → Settings → Secrets and variables → Actions
2. New repository secret
3. Название: `FIREBASE_TOKEN`
4. Значение: токен из шага 1
5. Add secret

### Шаг 4: Загрузите проект в GitHub

Загрузите **весь проект**, включая:
- `.github/` папку (в корне!)
- `.firebaserc` файл (в корне!)
- `functions/` папку
- Все остальные файлы

### Шаг 5: Запустите развертывание

1. GitHub → Actions
2. Выберите workflow "Deploy Firebase Functions"
3. Нажмите "Run workflow"
4. Выберите ветку (обычно `main`)
5. Run workflow

## ✅ Проверка

После развертывания:
1. GitHub → Actions → должна быть зеленая галочка ✅
2. Firebase Console → Functions → функция `cleanupExpiredPosts` должна быть активна

## 📚 Дополнительная информация

- См. `SETUP_INSTRUCTIONS.md` для подробной инструкции
- См. `../QUICK_START_GITHUB.md` для быстрого старта
- См. `../FREE_FIREBASE_SETUP.md` для информации о бесплатном тарифе

