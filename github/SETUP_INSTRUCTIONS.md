# 📋 Подробная инструкция по настройке GitHub Actions

## ✅ Важно: Firebase Functions БЕСПЛАТНЫЕ!

**Firebase Cloud Functions имеют бесплатный тариф:**
- 2 миллиона вызовов функций в месяц - **БЕСПЛАТНО**
- Для вашей задачи (очистка 1 раз в день) = **30 вызовов/месяц**
- Это **0.001%** от бесплатного лимита
- **Вы не будете платить!**

## 🎯 Что нужно сделать

### Шаг 1: Подготовка (когда есть интернет)

#### 1.1 Установите Node.js (если еще не установлен)

Скачайте с [nodejs.org](https://nodejs.org/) и установите.

#### 1.2 Установите Firebase CLI

```bash
npm install -g firebase-tools
```

#### 1.3 Получите Firebase Token

```bash
firebase login:ci
```

**Важно:** Скопируйте полученный токен! Он понадобится для шага 3.

Пример токена:
```
1//xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

### Шаг 2: Создайте GitHub репозиторий

1. Откройте [GitHub](https://github.com/)
2. Войдите в свой аккаунт
3. Нажмите **"New"** или **"+"** → **"New repository"**
4. Заполните:
   - **Repository name**: `imtahan-plus` (или любое другое имя)
   - **Description**: `İmtahan+ - Təhsil və imtahan platforması`
   - **Public** (важно! для бесплатного GitHub Actions)
   - **НЕ** ставьте галочку "Initialize this repository with a README"
5. Нажмите **"Create repository"**

### Шаг 3: Добавьте Firebase Token в GitHub Secrets

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** (вверху справа)
3. В левом меню найдите **Secrets and variables** → **Actions**
4. Нажмите **"New repository secret"**
5. Заполните:
   - **Name**: `FIREBASE_TOKEN`
   - **Secret**: вставьте токен из шага 1.3
6. Нажмите **"Add secret"**

### Шаг 4: Загрузите проект в GitHub

#### Вариант А: Через веб-интерфейс GitHub

1. На странице репозитория нажмите **"uploading an existing file"**
2. Загрузите все файлы проекта:
   - `.github/` папку (важно!)
   - `.firebaserc` файл (важно!)
   - `functions/` папку
   - Все остальные файлы

#### Вариант Б: Через Git (если установлен)

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/imtahan-plus.git
git push -u origin main
```

### Шаг 5: Проверьте структуру файлов

Убедитесь, что в репозитории есть:

```
.github/
└── workflows/
    └── deploy-functions.yml    ← Должен быть в корне!

.firebaserc                     ← Должен быть в корне!

functions/
├── index.js
└── package.json
```

### Шаг 6: Запустите развертывание

#### Вариант А: Автоматически

Просто загрузите изменения в папку `functions/` - GitHub Actions автоматически развернет функцию.

#### Вариант Б: Вручную

1. Откройте GitHub → **Actions**
2. Выберите workflow **"Deploy Firebase Functions"**
3. Нажмите **"Run workflow"**
4. Выберите ветку (обычно `main`)
5. Нажмите зеленую кнопку **"Run workflow"**

### Шаг 7: Проверьте развертывание

#### В GitHub:

1. GitHub → **Actions**
2. Выберите последний workflow run
3. Должна быть зеленая галочка ✅
4. Если есть ошибки, проверьте логи

#### В Firebase Console:

1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект `imtahan-plus1`
3. Перейдите в **Functions**
4. Найдите функцию `cleanupExpiredPosts`
5. Статус должен быть **"Активна"**

## 🔍 Проверка работы

После первого запуска проверьте:

1. **GitHub Actions**: Должна быть зеленая галочка ✅
2. **Firebase Console** → Functions → `cleanupExpiredPosts` → Logs
3. Здесь вы увидите логи выполнения функции

## ❓ Решение проблем

### Ошибка: "FIREBASE_TOKEN not found"

**Решение:**
- Проверьте, что токен добавлен в GitHub Secrets
- Название должно быть точно `FIREBASE_TOKEN` (без пробелов)
- Перезапустите workflow

### Ошибка: "Project not found"

**Решение:**
- Проверьте файл `.firebaserc` - проект должен быть `imtahan-plus1`
- Если проект другой, измените в `.firebaserc` и в workflow файле

### Ошибка: "Functions deploy failed"

**Решение:**
- Проверьте логи в GitHub Actions
- Убедитесь, что все зависимости установлены в `functions/package.json`
- Проверьте, что токен правильный

### Ошибка: "Permission denied"

**Решение:**
- Проверьте, что токен получен правильно через `firebase login:ci`
- Убедитесь, что у токена есть права на проект

## 💡 Советы

1. **Публичный репозиторий** - GitHub Actions бесплатный для публичных репозиториев
2. **Токен безопасен** - он хранится в GitHub Secrets, никто его не увидит
3. **Автоматическое развертывание** - при изменении `functions/` функция обновится автоматически
4. **Мониторинг** - логи доступны в GitHub Actions и Firebase Console

## 📊 После настройки

После настройки:
- ✅ Функция работает автоматически на сервере Firebase
- ✅ Вам больше не нужен интернет на ПК
- ✅ Функция запускается каждый день по расписанию (3:00 UTC)
- ✅ Все бесплатно!

## 🎯 Следующие шаги

1. ✅ Настройте GitHub Actions (один раз)
2. ✅ Загрузите код в GitHub
3. ✅ Разверните функцию
4. ✅ Готово! Функция работает автоматически

