# İmtahan+ Firebase Functions

Репозиторий для Firebase Cloud Functions приложения İmtahan+.

## 📁 Структура проекта

```
.
├── .github/
│   └── workflows/
│       └── deploy-functions.yml    # GitHub Actions для автоматического развертывания
├── .firebaserc                     # Конфигурация Firebase проекта
├── functions/
│   ├── index.js                    # Основной файл с функциями
│   ├── package.json                # Зависимости Node.js
│   ├── .gitignore                  # Игнорируемые файлы
│   └── .eslintrc.js                # Конфигурация ESLint
├── firebase.json                   # Конфигурация Firebase
└── README.md                        # Этот файл
```

## 🚀 Функции

### 1. `sendPushNotification`
Автоматически отправляет push-уведомления через FCM при создании документа в `push_notifications_queue`.

### 2. `cleanupNotificationQueue`
Очищает старые записи из очереди уведомлений. Запускается каждый день в 2:00 UTC.

### 3. `cleanupExpiredPosts`
Автоматически удаляет истекшие посты форума (30 дней). Запускается каждый день в 3:00 UTC (7:00 по Баку).

### 4. `sendTestNotification`
HTTP функция для ручной отправки тестового уведомления.

## 📦 Развертывание

### Автоматическое развертывание через GitHub Actions

1. Добавьте Firebase Token в GitHub Secrets:
   - Settings → Secrets and variables → Actions
   - Name: `FIREBASE_TOKEN`
   - Value: ваш токен (получите через `firebase login:ci`)

2. Запустите workflow:
   - GitHub → Actions → "Deploy Firebase Functions" → Run workflow

### Ручное развертывание

```bash
npm install -g firebase-tools
firebase login
firebase deploy --only functions
```

## 📚 Документация

См. папку `github/` для подробных инструкций по настройке.

